;; Stage 01 Core Interface Registry — EMPTY BY DESIGN. Nothing is declared in this file.
;; Module-owned interfaces live in each 2_Core/*.pact file and deploy with their module
;; (StoicSyntax-Prefixes.md §7.10, co-location, 2026-09-02). The header used to say
;; "SHARED + HISTORICAL only"; as of that amendment it holds neither, and a registry that
;; describes contents it no longer has reads exactly like one that is correctly populated.
;; Do not add interfaces here — add them to the module file instead. See DEFECT-LEDGER §8.6.
