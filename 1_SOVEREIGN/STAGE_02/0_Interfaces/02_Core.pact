;; Stage 02 Core Interface Registry — SHARED schemas only.
;; DpdcUdcV2 types are referenced by all DPDC slice interfaces — keep here.
;; Module-owned latest interfaces live in 2_Core/**/*.pact (deploy with module).
;;