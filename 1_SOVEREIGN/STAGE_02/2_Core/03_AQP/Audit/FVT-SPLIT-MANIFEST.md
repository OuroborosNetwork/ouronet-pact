# FVT-SPLIT-MANIFEST — verified relocation list (P1)

Generated by static call-graph analysis (`REPL/_fvtsplit.py`) over `04_FVT.pact`.
**Key result: 0 functions directly touch both domains** → the seam is clean; no atomic
cross-domain write-transaction needs untangling. All coupling is at the call-graph level.

## Classification (transitive)
| class | count | disposition |
|---|---|---|
| **RPS** (touches only RPS tables, transitively) | 79 | **move to `RPS`** |
| **FVT** (touches only FVT tables) | 164 | stay in `FVT` |
| **SEAM** (touches both, transitively) | 77 | **stay in `FVT`**, rewire RPS calls → `ref-RPS::` |
| **FREE** (no table) | 87 | follow their sole caller; pure `UC_` may duplicate |

## Move to `RPS` — tables (7) + schemas (7)
- Tables: `FVT|T|RPS|Global`, `FVT|T|RPS|Member`, `FVT|T|RPS|User`, `FVT|T|RPS|Stream`,
  `FVT|T|MemberVault`, `FVT|T|MemberUserWeight`, `FVT|T|ForcedFixCount`
- Schemas: `FVT|RPS|Global`, `FVT|RPS|Member`, `FVT|RPS|User`, `FVT|RPS|Stream`,
  `FVT|MemberVault`, `FVT|MemberUserWeight`, `FVT|ForcedFixCount`
- Caps: **own `SECURE`** (RPS defines its own, composed by its writers) + `P|SECURE-CALLER`
  + `FVT|XE>DISPOSE-ROYALTY`
- Policy plumbing duplicated: `P|T`, `P|MT`, IMC (`P|UEV_IMC`, `P|A_Define`, `P|I`)

## Move to `RPS` — the 79 functions (by band)
- **Readers** `UR_FVT-RG|* / -RM|* / -RU|* / -RS|* / -MV|* / -MUW|* / -FFC|*` (ledger fields)
- **Derivers** `URC_FvtRps{Global,User,Member}RowExists`, `URC_ReleasableToNow`,
  `URC_StreamStatus`, `URC_ScoreEntityUserWeight`, `URC_StakeAnyPendingOnFvtRewardLine`,
  `URC_CollectForcedFixIgnis`, `URH_FVT-RG|EnabledRewardRows`, `URH_FVT|SettleFvtRewardBundle`
- **Writers** `WW_MemberUserWeight`, `WU_MemberVault|*`, `WU_FvtForcedFixCount|*`,
  `WI_RpsGlobal`, `WU_RpsGlobal|*`, `WW_RpsStream`, `WI_RpsMember`, `WW_RpsMember`,
  `WI_RpsUser`, `WU_RpsUser|*`
- **Book/ensure internals** `XI_BookCollectUnclaimed`, `XI_1|BookCollectUnclaimed`,
  `XI_2|EnsureRpsMemberRow`, `XI_2|BumpRpsGlobalUnclaimed`, `XI_1|BookUnclaimedForFvtRewardLine`,
  `XI_BookStakeUnclaimedCounts`
- **Royalty custody** `XE_WithdrawRoyalty`, `XE_BurnRoyalty`, `XE_FuelRoyalty`,
  `XE_BookStakeUnclaimedCounts`, `URCi_{Withdraw,Burn,Fuel}RoyaltyCustody`
  → **NOTE:** DSA consumes these; after the split DSA references `RPS::` for royalty ops
  (currently `AQP-FVT::`). One extra line in the DSA cascade.

(Full name list: `REPL/_fvtsplit.py list`.)

## `XE_` boundary (RPS exposes; FVT calls)
The 77 SEAM orchestrators keep their structure logic and call RPS through `XE_`/reader refs.
The RPS-write portions already isolated in the 79 above become the `XE_` surface. Hot-loop
settle/inject bodies that today read FVT structure per-iteration get the structure **arg-passed**
so the loop runs entirely inside RPS (one hop per phase — §5 of the design).

## DAG check
`RPS` functions directly reading an `FVT` table: **NONE**. `RPS` is a clean leaf →
deploys first. Any exception found during surgery = convert that read to an argument.

---

## P1 boundary spec (generated by `_fvtgen.py`)

Full move-set (with FREE-helper closure): **97 defs** — 1,231 lines of functions + 7 schemas
(111 lines) + 7 tables. Constants: `AQP|SC_NAME`, `BAR`, `CT_REWARD_KIND_PLAIN`,
`CT_SCORE_ENTITY_TRIPLET`, `P|I` are FVT-local (copy); `CT_FORCED_FIX_RATE`, `STREAM_EPOCH`
resolve elsewhere (pin their real defs at assembly).

### Public surface = 59 moved fns called by staying (FVT/SEAM) members
- **37 readers** (`UR_/URC_/URH_/UC_/UCk_/UDC_`) → expose directly; FVT calls `ref-RPS::` (reads need no SECURE).
- **21 writers** need an `XE_` entrypoint (FVT cannot acquire RPS's `SECURE`):
  `WI_RpsGlobal/Member/User`, `WU_RpsGlobal|{RewardEnabled,CurrentRps,AvailableRewards,UnclaimedCount,ZombieRewards,RoyaltyRewards,StreamCount,StreamLastRelease,StreamUnreleased}`,
  `WU_RpsUser|{LastRps,PendingRewards}`, `WU_MemberVault|AvailableRewards`,
  `WU_FvtForcedFixCount|{Add,Zero}`, `WW_MemberUserWeight`, `WW_RpsMember`, `WW_RpsStream`,
  `XI_2|EnsureRpsMemberRow`, `XI_BookCollectUnclaimed`.

### The wrapper-vs-loop fork (key implementation decision)
Wrapping each writer in a thin `XE_` is correct but puts a cross-module hop **inside** FVT's
settle/inject loops (per-iteration gas). The design target is loops **inside** RPS (one hop/phase).
**Plan:** build correctness-first with thin `XE_` wrappers → green-gate → run `[6.2.6]` GAS probe →
promote only the specific hot loops that breach budget into RPS as consolidated `XE_` (arg-passing
the FVT structure). Measure-driven, not blanket — avoids over-refactoring cold paths.

### Assembly order (next work block)
1. `AcquisitionRewardPerShareV1` interface (37 readers + 21 `XE_` + `URCi_` royalty + policy members).
2. `04_RPS.pact`: header (GOV/policy/`SECURE`/IMC, `P|FVT|→P|RPS|` renamed) + consts + 7 schemas +
   7 tables + 97 bodies + 21 `XE_` wrappers. Validate load in an isolated probe repl.
3. FVT surgery: delete moved defs; rewire 59 call sites → `ref-RPS::`; add `RPS` module-ref binding.
4. Renumber `04_RPS`/`05_FVT`/… + executor deploy-list + FVT `P|A_Define` registers on RPS (IMC).
5. `Z.repl` + `[6.2.4]`/`[6.2.5]` green; re-measure; then the GAS-probe pass.

---

## P1 conclusion (2026-09-03): only B' (balanced reward-engine split) is clean

Empirically established over P1: the accessor-only split (ledger + thin writers → RPS) is NOT
cleanly completable and leaves FVT lopsided (6,435 ln / ~84% gas):
- The reward orchestration is pervasively coupled to BOTH the RPS ledger AND FVT structure.
- Staying orchestration functions read moved RPS data across the object boundary
  (`URH_FVT|SettleFvtRewardBundle` → `[object{FVT|SettleFvtRewards}]`; `UR_FVT-RS|Stream` →
  `object{FVT|RPS|Stream}`), which the interface object-return rule bars — forcing those
  functions into RPS anyway.
- The orchestration also writes 5 `FVT|T` aggregate fields (would be RPS→FVT cycle).

**Required path = B'** (design §2–5, revised): reward ENGINE (ledger + distribution topology
ScoreEntityLink/UserPresence/MultipletFamily + new `FVT|T|RewardAggregate` + the ~195-fn
orchestration) → RPS; FVT keeps entity/config/client and arg-passes `FVT|T` config into the RPS
phase entrypoints. Measured target RPS ≈ 3,288 / FVT ≈ 2,765, DAG-clean. This is a major,
correctness-sensitive surgery (reward math) and should run staged with a behavioral-equivalence
gate per step (Stage 1 FVT|T field split → Stage 2 config arg-threading → Stage 3 module move),
each green-gating in-module before the boundary flip.

**Assets ready for B' execution:** validated RPS module+interface builder (`_fvtasm.py`, loads at
~120K gas), call-graph classifier (`_fvtsplit.py`), body extractor (`_fvtgen.py`), cutter
(`_fvtcut.py`) — all re-point to the wider table scope by editing the `RPS_TBL` set.
