# Skills (Ouronet)

Place **repeatable procedures** here as small markdown files (e.g. `running-the-repl.md`, `adding-a-stage.md`).

**Cursor Agent Skill:** `.cursor/skills/ouronet-ur-layout/SKILL.md` — how to order **`UR_*`** helpers (schema groups, field order, multi-table dispatch before per-table implementations). Authoritative detail: `OuronetInformational/MODULE_ARCHITECTURE.md`.
