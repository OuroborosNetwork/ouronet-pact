# Deep-dive all modules index (2026-04-07)

Added:

- `OuronetInformational/ouronet/architecture/DEEP_DIVE_ALL_MODULES.md`

Content includes:

- stage-level totals (`defun`, `defcap`, `deftable`, `defschema`),
- prefix distributions (`UC_`, `UR_`, `URC_`, `UEV_`, `UDC_`, `CAP_`, `A_`, `C_`, `X*`),
- top modules by function count,
- relationship map (Stage 1 backbone, Stage 2 DPDC + DEMIPAD topology, slave/Stage_Z role),
- functional-domain matrix and next deepening steps for whitepaper writing.

Notes:

- Counts are from static file indexing and include caveats for files that co-locate interfaces and modules.
- This index is a baseline; next passes can add explicit call-flow diagrams and per-module invariants.
