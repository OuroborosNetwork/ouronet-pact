# pact 5.4.1 binary — REPL test runner upgrade (2026-08-27)

**Why:** a super-critical exploit was fixed in pact **5.4.1**. All REPL testing must use it.

**What changed on the dev box:**
- `~/.local/bin/pact` is now **5.4.1** (downloaded from
  `github.com/kda-community/pact-5` releases → `pact-5.4.1-72f4-ubuntu-24.04.tar.gz`;
  runs fine on Ubuntu 26.04).
- the previous **5.4** binary is preserved as `~/.local/bin/pact-5.4` (fallback only).
- Verify with `pact --version` → `pact version 5.4.1`.

**Validation:** the full `REPL/run-aqp-audit.sh` (15 suites) is **ALL GREEN** on 5.4.1.

New contributors: install the same 5.4.1 ubuntu-24.04 asset onto PATH before running any
`.repl`. Do NOT test on 5.4 (the exploit is unpatched there).
