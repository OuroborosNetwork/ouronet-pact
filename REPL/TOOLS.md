# REPL analysis tooling — INDEX

Generated by `python3 _toolindex.py` from each script's first docstring line. Regenerate after
adding a tool.

**Read this before writing a new analysis script.** There are 33 here, and duplicates have been
built more than once — an eager-let detector, a write-leak auditor and a Pact comment stripper have
each been re-derived by someone who did not know the original existed. The stripper in particular
is why `_pactlex.py` exists: import from it rather than re-writing `strip_comments`, which has
shipped broken (Pact `@doc` strings continue across lines with a trailing `\`, and a naive escape
parser desyncs on them) at least four times.

| script | what it answers |
|---|---|
| `_audit_modref_calls.py` | (no docstring) |
| `_cheapseam.py` | Remaining unpinned guards that sit in a PLAIN callable defun (UEV_/URCv_/UCv_/UC_/URC_). |
| `_conformance.py` | STATIC CONFORMANCE LINTER — does the code obey the prefix contracts it claims? |
| `_deadbind.py` | DEAD LET-BINDINGS — a `let` binding that COMPUTES something and is then never read. |
| `_deadguard.py` | DEAD-GUARD PRE-PASS (plan step 3.4, run BEFORE 3.3) — which `enforce`s can never fire? |
| `_docclaims.py` | UNVERIFIED @doc CLAIMS — a function whose doc promises a RULE that no test pins. |
| `_eagerlet.py` | [eager-let] — a guard whose own SUBJECT is hard-read in the let binding group above it. |
| `_enforce_coverage.py` | ENFORCE COVERAGE (plan step 3.2) — which guards has a test actually PINNED? |
| `_expectfail.py` | EXPECT-FAILURE STRENGTH (plan step 3.1) — how many negative tests accept ANY error? |
| `_foldeager.py` | [fold-eager] — a (fold (and) true [...]) whose LATER conjunct consumes a value an EARLIER |
| `_fvtasm.py` | _fvtasm.py — assemble a first-draft 04_RPS.pact from 04_FVT.pact spans + a hand header. |
| `_fvtcut.py` | _fvtcut.py — remove the RPS-exclusive defs/tables/schemas from 04_FVT.pact (in place). |
| `_fvtfacade.py` | (no docstring) |
| `_fvtflip.py` | _fvtflip.py — Stage 3 flip: cut RPS-domain from 04_FVT.pact, rewire calls to ref-RPS::, |
| `_fvtgen.py` | (no docstring) |
| `_fvtsplit.py` | (no docstring) |
| `_gate.py` | THE GATE — the one runner that decides whether the REPL suite is green. |
| `_heavy.py` | HEAVY-PREFIX CHECKER (G4 / plan step 2.5.3) — is the DOUBLED prefix telling the truth? |
| `_ignis_cost_classify.py` | (no docstring) |
| `_ignis_cost_inventory.py` | (no docstring) |
| `_ignis_deter_worksheet.py` | #76 — deterrence worksheet: per client/admin op, approx COMPUTE (module-internal transitive |
| `_ignis_price_sheet.py` | #76 — IGNIS PRICE SHEET, built from the TALOS client surface. |
| `_ladder.py` | BATCH LADDER TILING — the citizen minters' `A_StepNN`/`A_FixNN` literal mint plans must tile each collection exactly once, within the 70-position transaction budget, with Fix matching Spawn. Gate-enforced. |
| `_leakaudit.py` | Which PINNED guards sit AFTER a persisting write inside the same function body? |
| `_leakaudit_xmod.py` | CROSS-MODULE version of the leak audit. |
| `_letfix.py` | (no docstring) |
| `_normalize_repl_layout.py` | Normalize Ouronet REPL/*.repl to canonical layout. Skips 6.2.1 ANK and 6.2.2 SCORE. |
| `_orphanmatch.py` | Which ORPHAN assertions actually correspond to a real source guard? |
| `_p33_classify.py` | P3.3 residue classifier — WHY each remaining guard is unpinned, not just that it is. |
| `_pactlex.py` | Shared Pact lexing helpers for the analysis scripts. |
| `_scale_report.py` | SCALE OF THE REPL TEST SUITE — size, reach, and per-function exercise counts. |
| `_shadowed.py` | SHADOWED GUARDS — enforces that can never fire because a hard read runs first. |
| `_stagez_variant.py` | Generate the REPL-only Stage-Z TESTING VARIANT from the canonical module (never edit canonical). |
| `_subdivide_repl.py` | (no docstring) |
| `_suite_stats.py` | SUITE STATS -> one Markdown report. Composes the other tools; computes nothing itself. |
| `_test_ledger.py` | REPL TEST LEDGER — every client function, how often it is tested, where, and how. |
| `_tighten.py` | TIGHTEN weak `expect-failure`s to the 3-arg form, by HARVESTING the real message. |
| `_toolindex.py` | Regenerate TOOLS.md — an index of every REPL analysis script, from its first docstring line. |
| `_vacuous.py` | VACUOUS / WEAK POSITIVE ASSERTIONS — which `(expect ...)` cannot fail, or barely can? |
| `_xprotect.py` | X_ PROTECTION CLASSES — derive, verify and emit the `;;Protection:` line. |
